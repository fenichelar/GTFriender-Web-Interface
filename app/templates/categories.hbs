<h1>Categories</h1>
<ul>
    {{#each model as |category|}}
        <li>
            {{#link-to "categories.category.activities" category.id}}
                {{category.name}}
            {{/link-to}}
        </li>
    {{/each}}
    {{outlet}}
</ul>
